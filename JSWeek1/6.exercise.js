const myArrays = [];
console.log('Right now the value of myArray must be \'\' because obviously a variable exists but it doesn\'t contain a value yet. And its index value must be -1. Beacuse there is no element to find and execute its value.');
console.log(myArrays);
console.log(myArrays.indexOf());
const myFavoriteAnimalsContainer = ['horse','cat','dolphin','dog'];
console.log(myFavoriteAnimalsContainer);
myFavoriteAnimalsContainer.push('baby pig');
console.log(myFavoriteAnimalsContainer);
